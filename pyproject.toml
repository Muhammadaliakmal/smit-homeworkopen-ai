[project]
name = "gemini-openai-agent"
version = "0.1.0"
description = "One Agent definition, two providers (OpenAI & Gemini), switched via .env"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai-agents>=0.20.0",
    "python-dotenv>=1.2.2",
]
